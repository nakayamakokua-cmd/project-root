# Codex プロジェクト文脈（固定ロード）
このリポジトリでは「音源分析 基本設計（拡張MVP版）」に基づいた実装・保守を行います。
必ず下記の設計/辞書/スクリプトを“参照・更新”してください。

## 参照ドキュメント
- docs/音源分析_基本設計_MVP.md  … 全体アーキテクチャ/指標/NG分類/算出ルール/バッチ運用
- Profiles/talk_script_v1.md      … トークスクリプト（#1〜#6）
- config/ng_master.json           … NG分類マスタ（大分類1〜6 / 中分類）※出力は日本語名で
- config/script_master.json       … 分岐(#2→#3-x)・適合度（E1〜E4, G）ルール
- config/settings.json            … 各種しきい値/許容度（strict=0.7 等）

## 不変方針
- スプレッドシート行出力に **ng_major / ng_minor / ng_reason_chain（日本語, 時系列カンマ区切り）** を必ず含める。
- 「分岐パターン」は **最初の顧客反応で確定**（途中遷移は履歴に記録のみ）。
- 最終NGは **終話ウィンドウ（既定60秒）**を優先。該当なし時は **確定度**で決める。
- 解析は **ステップA=構造化抽出 → ステップB=根拠抽出/コーチング生成** の2段。
- バッチは Mac ローカル完結。失敗は /Audio/error 退避＋ログ保存。429/5xx は指数バックオフ。

## 自動ドキュ更新（運用規約）
- コードの仕様変更を行った場合、**同一PR内で**：
  1) 該当スクリプト修正
  2) マスタ/設定(JSON)の同期更新
  3) 設計書( docs/音源分析_基本設計_MVP.md )の差分追記
- 例外なく、Codex は“設計と実装の乖離”を検出したら **修正案 or ドキュ追記** を必ず出すこと。

## 作業ガイド
- 解析器は `scripts/analyzer.py`、シート書込は `scripts/sheets_writer.py`
- `ng_reason_chain` は（例）「営業一律お断り, 親会社・系列による統一運用」のように**出現順**で格納。
- 電話番号はファイル名第2トークンから抽出→**0始まり・ハイフン無し**に正規化、桁異常はフラグ。

## 継続作業の入口
- ロードマップ: `docs/ROADMAP.md`（Next/Doing/Done/決定履歴）
- バックログ: `docs/BACKLOG.json`（機械可読のタスク一覧。優先度/期日/状態/担当）
- Codex文脈: `codex/context/*.md`（`product_context.md`/`ARCHITECTURE.md`/`_analysis_mvp_design.md`/`_recent_changes.md`）
- 本ドキュ（AGENTS.md）を入口として維持し、設計は `docs/音源分析_基本設計_MVP.md` に差分追記する運用とする

### 補足
- 非エンジニア向けガイド: `docs/クイックガイド_非エンジニア向け.md`
